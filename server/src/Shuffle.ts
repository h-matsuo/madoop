import Method from './Method';
import Language from './Language';

class Shuffle extends Method {

  /*
  constructor(language?: Language,
              source?: string,
              js?: Function,
              wasm?: Uint8Array) {
    super(language, source, js, wasm);
  }
  */

}

export default Shuffle;
